{
  "name": "connecto-desktop",
  "private": true,
  "version": "2.0.0",
  "description": "Windows desktop client for CorNet with mirror failover for restricted networks",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win",
    "release": "electron-builder --win --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0"
  }
}
